import { type WebSocket } from 'ws';
import { type DevServerInfo, type ModuleDescriptor } from '../shared/protocol';
export interface DynamicToolEntry {
    description: string;
    module: string;
    inputSchema?: Record<string, unknown>;
}
export interface ClientEntry {
    readonly connectedAt: number;
    readonly dynamicTools: Map<string, DynamicToolEntry>;
    readonly id: string;
    modules: ModuleDescriptor[];
    readonly socket: WebSocket;
    readonly appName?: string;
    readonly appVersion?: string;
    readonly bundleId?: string;
    readonly devServer?: DevServerInfo;
    readonly deviceId?: string;
    readonly isSimulator?: boolean;
    readonly label?: string;
    readonly platform?: string;
}
export type ClientResolution = {
    client: ClientEntry;
    ok: true;
} | {
    error: string;
    ok: false;
};
export declare class Bridge {
    private readonly port;
    private wss;
    private clients;
    private disconnectedClients;
    private socketToClientId;
    private platformSequences;
    private pendingRequests;
    constructor(port: number);
    start(): Promise<void>;
    stop(): Promise<void>;
    call(clientId: string, module: string, method: string, args: Record<string, unknown>, timeout?: number): Promise<unknown>;
    isAnyClientConnected(): boolean;
    listClients(): ClientEntry[];
    getClient(id: string): ClientEntry | undefined;
    resolveClient(explicitId?: string): ClientResolution;
    private handleMessage;
    private clientForSocket;
    private nextClientId;
    private findGhostMatch;
    private rejectPendingForClient;
    private rejectAllPending;
}
//# sourceMappingURL=bridge.d.ts.map