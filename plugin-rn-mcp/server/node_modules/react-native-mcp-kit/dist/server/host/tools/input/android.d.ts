import { type AppTargetError } from '../../../../server/host/helpers';
import { type ProcessRunner } from '../../../../server/host/processRunner';
export declare const tapAndroid: (serial: string, x: number, y: number, runner: ProcessRunner) => Promise<{
    ok: true;
} | AppTargetError>;
export declare const swipeAndroid: (serial: string, x1: number, y1: number, x2: number, y2: number, durationMs: number, runner: ProcessRunner) => Promise<{
    ok: true;
} | AppTargetError>;
export declare const typeTextAndroid: (serial: string, text: string, submit: boolean, runner: ProcessRunner) => Promise<{
    ok: true;
} | AppTargetError>;
export declare const pressKeyAndroid: (serial: string, key: string, runner: ProcessRunner) => Promise<{
    ok: true;
} | AppTargetError>;
//# sourceMappingURL=android.d.ts.map