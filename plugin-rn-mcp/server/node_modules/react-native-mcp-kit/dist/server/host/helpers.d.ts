export interface AppTargetError {
    error: string;
}
export declare const NATIVE_ID_SCHEMA: {
    readonly serial: {
        readonly description: "Optional explicit adb serial of the target Android device (e.g. \"emulator-5554\"). Highest priority — bypasses clientId and platform-based device selection. Use values from host__list_devices output.";
        readonly type: "string";
    };
    readonly udid: {
        readonly description: "Optional explicit simctl UDID of the target iOS simulator. Highest priority — bypasses clientId and platform-based device selection. Use values from host__list_devices output.";
        readonly type: "string";
    };
};
export declare const PLATFORM_ARG_SCHEMA: {
    readonly description: "Optional platform filter: \"ios\" or \"android\". Ignored when clientId is provided on the outer call tool (the client's own platform is used instead).";
    readonly enum: readonly ["android", "ios"];
    readonly type: "string";
};
export declare const parsePlatformArg: (value: unknown) => "android" | "ios" | undefined;
export declare const parseStringArg: (value: unknown) => string | undefined;
export declare const parseResolveOptions: (args: Record<string, unknown>) => {
    platform?: "android" | "ios";
    serial?: string;
    udid?: string;
};
export declare const parseCoord: (value: unknown, name: string) => {
    ok: true;
    value: number;
} | {
    error: string;
    ok: false;
};
//# sourceMappingURL=helpers.d.ts.map