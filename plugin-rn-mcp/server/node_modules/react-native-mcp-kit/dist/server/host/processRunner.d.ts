export interface ProcessResult {
    exitCode: number | null;
    signal: NodeJS.Signals | null;
    stderr: Buffer;
    stdout: Buffer;
    timedOut: boolean;
}
export interface RunProcessOptions {
    env?: Record<string, string>;
    killSignal?: NodeJS.Signals;
    timeoutMs?: number;
}
export declare class ProcessNotFoundError extends Error {
    readonly command: string;
    constructor(command: string);
}
export type ProcessRunner = (command: string, args: readonly string[], options?: RunProcessOptions) => Promise<ProcessResult>;
export declare const runProcess: ProcessRunner;
//# sourceMappingURL=processRunner.d.ts.map