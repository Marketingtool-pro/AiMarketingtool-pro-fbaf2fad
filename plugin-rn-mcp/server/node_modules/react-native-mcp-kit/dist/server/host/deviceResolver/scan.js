"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.scanForDevice = void 0;
const list_1 = require("./list");
const scanIosDevices = async (runner) => {
    try {
        const sims = await (0, list_1.listIosSimulators)(runner);
        const booted = sims.filter((s) => {
            return s.state === 'Booted';
        });
        if (booted.length === 0) {
            return {
                error: 'No booted iOS simulator found. Boot one via Simulator.app or `xcrun simctl boot <udid>`.',
                ok: false,
            };
        }
        if (booted.length === 1) {
            const only = booted[0];
            return {
                device: {
                    displayName: only.name,
                    kind: 'simulator',
                    nativeId: only.udid,
                    platform: 'ios',
                },
                ok: true,
            };
        }
        const list = booted
            .map((s) => {
            return `${s.name} (${s.udid})`;
        })
            .join(', ');
        return {
            error: `Multiple iOS simulators booted: ${list}. Specify clientId or a more precise target.`,
            ok: false,
        };
    }
    catch (err) {
        return (0, list_1.toDeviceError)(err, 'Failed to list iOS simulators', list_1.XCRUN_MISSING_ERROR);
    }
};
const scanAndroidDevices = async (runner) => {
    try {
        const devices = await (0, list_1.listAndroidDevices)(runner);
        const online = devices.filter((d) => {
            return d.state === 'device';
        });
        if (online.length === 0) {
            return {
                error: 'No online Android device found. Start an emulator or connect a device.',
                ok: false,
            };
        }
        if (online.length === 1) {
            const only = online[0];
            return {
                device: {
                    displayName: only.serial,
                    kind: 'real-device',
                    nativeId: only.serial,
                    platform: 'android',
                },
                ok: true,
            };
        }
        const list = online
            .map((d) => {
            return d.serial;
        })
            .join(', ');
        return {
            error: `Multiple Android devices online: ${list}. Specify clientId.`,
            ok: false,
        };
    }
    catch (err) {
        return (0, list_1.toDeviceError)(err, 'Failed to list Android devices', list_1.ADB_MISSING_ERROR);
    }
};
const scanForDevice = async (platform, runner) => {
    if (platform === 'ios') {
        return scanIosDevices(runner);
    }
    if (platform === 'android') {
        return scanAndroidDevices(runner);
    }
    const iosResult = await scanIosDevices(runner);
    if (iosResult.ok) {
        return iosResult;
    }
    const androidResult = await scanAndroidDevices(runner);
    if (androidResult.ok) {
        return androidResult;
    }
    return {
        error: `No device available. iOS: ${iosResult.error} Android: ${androidResult.error}`,
        ok: false,
    };
};
exports.scanForDevice = scanForDevice;
//# sourceMappingURL=scan.js.map