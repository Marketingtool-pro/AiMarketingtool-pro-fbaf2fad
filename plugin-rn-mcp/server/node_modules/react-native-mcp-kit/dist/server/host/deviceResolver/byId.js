"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveAndroidBySerial = exports.resolveIosByUdid = void 0;
const list_1 = require("./list");
const resolveIosByUdid = async (udid, runner) => {
    try {
        const sims = await (0, list_1.listIosSimulators)(runner);
        const match = sims.find((s) => {
            return s.udid === udid;
        });
        if (!match) {
            const available = sims
                .map((s) => {
                return `${s.udid} (${s.name})`;
            })
                .join(', ') || '(none)';
            return {
                error: `iOS simulator with UDID '${udid}' not found. Available: ${available}`,
                ok: false,
            };
        }
        if (match.state !== 'Booted') {
            return {
                error: `iOS simulator '${match.name}' (${udid}) is in state '${match.state}', not Booted. Boot it first via xcrun simctl boot.`,
                ok: false,
            };
        }
        return {
            device: {
                displayName: match.name,
                kind: 'simulator',
                nativeId: match.udid,
                platform: 'ios',
            },
            ok: true,
        };
    }
    catch (err) {
        return (0, list_1.toDeviceError)(err, 'Failed to list iOS simulators', list_1.XCRUN_MISSING_ERROR);
    }
};
exports.resolveIosByUdid = resolveIosByUdid;
const resolveAndroidBySerial = async (serial, runner) => {
    try {
        const devices = await (0, list_1.listAndroidDevices)(runner);
        const match = devices.find((d) => {
            return d.serial === serial;
        });
        if (!match) {
            const available = devices
                .map((d) => {
                return d.serial;
            })
                .join(', ') || '(none)';
            return {
                error: `Android device with serial '${serial}' not found. Available: ${available}`,
                ok: false,
            };
        }
        if (match.state !== 'device') {
            return {
                error: `Android device '${serial}' is in state '${match.state}', not 'device' (ready). Wait for boot to complete.`,
                ok: false,
            };
        }
        return {
            device: {
                displayName: match.serial,
                kind: 'real-device',
                nativeId: match.serial,
                platform: 'android',
            },
            ok: true,
        };
    }
    catch (err) {
        return (0, list_1.toDeviceError)(err, 'Failed to list Android devices', list_1.ADB_MISSING_ERROR);
    }
};
exports.resolveAndroidBySerial = resolveAndroidBySerial;
//# sourceMappingURL=byId.js.map