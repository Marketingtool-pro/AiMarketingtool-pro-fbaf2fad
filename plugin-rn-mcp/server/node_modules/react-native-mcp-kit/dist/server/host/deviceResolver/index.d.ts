import { type ProcessRunner } from '../../../server/host/processRunner';
import { type HostContext } from '../../../server/host/types';
import { type DeviceResolution } from './types';
interface ResolveOptions {
    platform?: 'android' | 'ios';
    serial?: string;
    udid?: string;
}
export declare const resolveDevice: (ctx: HostContext, options: ResolveOptions, runner: ProcessRunner) => Promise<DeviceResolution>;
export { clearDeviceCache, listAndroidDevices, listIosRealDevices, listIosSimulators, } from './list';
export { enrichDevicesWithClientStatus } from './enrich';
export { type AndroidDevice, type DeviceKind, type DeviceResolution, type EnrichedAndroidDevice, type EnrichedDeviceList, type EnrichedIosSim, type IosRealDevice, type IosSimulator, type ResolvedDevice, } from './types';
//# sourceMappingURL=index.d.ts.map