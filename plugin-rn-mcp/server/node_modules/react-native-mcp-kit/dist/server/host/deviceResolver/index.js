"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.enrichDevicesWithClientStatus = exports.listIosSimulators = exports.listIosRealDevices = exports.listAndroidDevices = exports.clearDeviceCache = exports.resolveDevice = void 0;
const byClient_1 = require("./byClient");
const byId_1 = require("./byId");
const scan_1 = require("./scan");
const resolveDevice = async (ctx, options, runner) => {
    // Step 0: explicit native identifier — highest priority, bypasses everything else
    if (options.udid) {
        return (0, byId_1.resolveIosByUdid)(options.udid, runner);
    }
    if (options.serial) {
        return (0, byId_1.resolveAndroidBySerial)(options.serial, runner);
    }
    // Step 1: explicit clientId — resolve it, no bare-scan fallback
    if (ctx.requestedClientId) {
        const client = ctx.bridge.getClient(ctx.requestedClientId);
        if (!client) {
            const available = ctx.bridge
                .listClients()
                .map((c) => {
                return c.id;
            })
                .join(', ') || '(none)';
            return {
                error: `Client '${ctx.requestedClientId}' not connected. Available: ${available}`,
                ok: false,
            };
        }
        return (0, byClient_1.resolveClientToDevice)(client, runner);
    }
    // Step 2: exactly one connected client matching the platform filter → auto-pick
    const clients = ctx.bridge.listClients();
    const filtered = options.platform
        ? clients.filter((c) => {
            return c.platform === options.platform;
        })
        : clients;
    if (filtered.length === 1) {
        return (0, byClient_1.resolveClientToDevice)(filtered[0], runner);
    }
    // Step 3: multiple matching clients → ambiguous, error
    if (filtered.length > 1) {
        const labels = filtered
            .map((c) => {
            return c.label ? `${c.id} (${c.label})` : c.id;
        })
            .join(', ');
        return {
            error: `Multiple clients connected: ${labels}. Specify clientId.`,
            ok: false,
        };
    }
    // Step 4: no clients matched — fall through to bare platform scan
    return (0, scan_1.scanForDevice)(options.platform, runner);
};
exports.resolveDevice = resolveDevice;
var list_1 = require("./list");
Object.defineProperty(exports, "clearDeviceCache", { enumerable: true, get: function () { return list_1.clearDeviceCache; } });
Object.defineProperty(exports, "listAndroidDevices", { enumerable: true, get: function () { return list_1.listAndroidDevices; } });
Object.defineProperty(exports, "listIosRealDevices", { enumerable: true, get: function () { return list_1.listIosRealDevices; } });
Object.defineProperty(exports, "listIosSimulators", { enumerable: true, get: function () { return list_1.listIosSimulators; } });
var enrich_1 = require("./enrich");
Object.defineProperty(exports, "enrichDevicesWithClientStatus", { enumerable: true, get: function () { return enrich_1.enrichDevicesWithClientStatus; } });
//# sourceMappingURL=index.js.map