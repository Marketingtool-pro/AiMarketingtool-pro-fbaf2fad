"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isLeafPredicate = exports.evalPredicate = exports.resolvePath = void 0;
/**
 * Drill into a value by dot-path. Arrays accept numeric indices and also
 * respond to `.length` (handy for "wait until list is empty"). Returns
 * undefined when any intermediate segment is missing.
 */
const resolvePath = (value, path) => {
    if (!path)
        return value;
    let current = value;
    for (const key of path.split('.')) {
        if (current == null)
            return undefined;
        if (Array.isArray(current)) {
            if (key === 'length') {
                current = current.length;
                continue;
            }
            const idx = Number.parseInt(key, 10);
            current = Number.isNaN(idx) ? undefined : current[idx];
            continue;
        }
        if (typeof current === 'object') {
            current = current[key];
            continue;
        }
        return undefined;
    }
    return current;
};
exports.resolvePath = resolvePath;
const evalLeaf = (actual, op, expected) => {
    switch (op) {
        case 'exists':
            return actual !== undefined && actual !== null;
        case 'notExists':
            return actual === undefined || actual === null;
        case 'equals':
            return Object.is(actual, expected);
        case 'notEquals':
            return !Object.is(actual, expected);
        case 'contains': {
            if (typeof actual === 'string' && typeof expected === 'string') {
                return actual.includes(expected);
            }
            if (Array.isArray(actual))
                return actual.includes(expected);
            return false;
        }
        case 'notContains': {
            if (typeof actual === 'string' && typeof expected === 'string') {
                return !actual.includes(expected);
            }
            if (Array.isArray(actual))
                return !actual.includes(expected);
            return false;
        }
        case 'gt':
            return typeof actual === 'number' && typeof expected === 'number' && actual > expected;
        case 'gte':
            return typeof actual === 'number' && typeof expected === 'number' && actual >= expected;
        case 'lt':
            return typeof actual === 'number' && typeof expected === 'number' && actual < expected;
        case 'lte':
            return typeof actual === 'number' && typeof expected === 'number' && actual <= expected;
        default:
            return false;
    }
};
/**
 * Evaluate a predicate (leaf or compound) against a result object. Compound
 * forms short-circuit: all stops on first false, any stops on first true.
 */
const evalPredicate = (result, predicate) => {
    if ('all' in predicate && Array.isArray(predicate.all)) {
        for (const sub of predicate.all) {
            if (!(0, exports.evalPredicate)(result, sub))
                return false;
        }
        return true;
    }
    if ('any' in predicate && Array.isArray(predicate.any)) {
        for (const sub of predicate.any) {
            if ((0, exports.evalPredicate)(result, sub))
                return true;
        }
        return false;
    }
    if ('not' in predicate && predicate.not && typeof predicate.not === 'object') {
        return !(0, exports.evalPredicate)(result, predicate.not);
    }
    const leaf = predicate;
    if (typeof leaf.op !== 'string')
        return false;
    return evalLeaf((0, exports.resolvePath)(result, leaf.path), leaf.op, leaf.value);
};
exports.evalPredicate = evalPredicate;
const isLeafPredicate = (pred) => {
    return typeof pred.op === 'string';
};
exports.isLeafPredicate = isLeafPredicate;
//# sourceMappingURL=predicate.js.map