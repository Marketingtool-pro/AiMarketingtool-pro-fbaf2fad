#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const host_1 = require("./host");
const processRunner_1 = require("./host/processRunner");
const metro_1 = require("./metro");
const index_1 = require("./index");
const args = process.argv.slice(2);
let port;
let disableHost = false;
for (let i = 0; i < args.length; i++) {
    if (args[i] === '--port' && args[i + 1]) {
        port = parseInt(args[i + 1], 10);
        i++;
    }
    else if (args[i] === '--no-host') {
        disableHost = true;
    }
}
(0, index_1.createServer)({
    hostModules: disableHost ? [] : [(0, host_1.hostModule)(processRunner_1.runProcess), (0, metro_1.metroModule)()],
    port,
}).catch((error) => {
    process.stderr.write(`Failed to start server: ${error.message}\n`);
    process.exit(1);
});
//# sourceMappingURL=cli.js.map