"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.serializeFiber = exports.getTextContent = void 0;
const constants_1 = require("./constants");
const naming_1 = require("./naming");
const getTextContent = (fiber) => {
    if (fiber.tag === constants_1.HOST_TEXT) {
        return fiber.memoizedProps;
    }
    // Check children for text
    let text = '';
    let child = fiber.child;
    while (child) {
        if (child.tag === constants_1.HOST_TEXT && typeof child.memoizedProps === 'string') {
            text += child.memoizedProps;
        }
        child = child.sibling;
    }
    return text || undefined;
};
exports.getTextContent = getTextContent;
// Host components that are just native mirrors of composite wrappers (e.g. RCTView for View)
const HOST_PASSTHROUGH = new Set(['RCTView', 'RCTText', 'RCTScrollView', 'RCTSafeAreaView']);
const shouldSkipFiber = (fiber) => {
    const componentType = (0, naming_1.getComponentType)(fiber);
    // Skip internal React wrapper nodes (providers, contexts, etc.)
    if (componentType === 'other' && fiber.tag !== constants_1.HOST_TEXT)
        return true;
    // Skip native host mirrors — their composite parent already represents the same component
    if (componentType === 'host' && HOST_PASSTHROUGH.has((0, naming_1.getComponentName)(fiber)))
        return true;
    return false;
};
const collectChildren = (fiber, maxDepth, currentDepth) => {
    const children = [];
    let child = fiber.child;
    while (child) {
        if (shouldSkipFiber(child)) {
            // Skip this node but collect its children at the same depth
            children.push(...collectChildren(child, maxDepth, currentDepth));
        }
        else {
            const serialized = (0, exports.serializeFiber)(child, maxDepth, currentDepth + 1);
            if (serialized) {
                children.push(serialized);
            }
        }
        child = child.sibling;
    }
    return children;
};
const serializeFiberUnsafe = (fiber, maxDepth, currentDepth) => {
    if (shouldSkipFiber(fiber)) {
        const children = collectChildren(fiber, maxDepth, currentDepth);
        if (children.length === 1)
            return children[0];
        if (children.length > 1) {
            return {
                children,
                name: 'Fragment',
                props: {},
                type: 'other',
            };
        }
        return null;
    }
    const name = (0, naming_1.getComponentName)(fiber);
    // Pass raw memoizedProps through — handler-level `applyProjection` runs the
    // single canonical projectValue walk on the final response, including this
    // tree. Projecting here would cause double-projection and break path drill.
    const props = (fiber.memoizedProps ?? {});
    const mcpId = fiber.memoizedProps?.['data-mcp-id'];
    const testID = fiber.memoizedProps?.testID;
    const text = (0, exports.getTextContent)(fiber);
    const children = collectChildren(fiber, maxDepth, currentDepth);
    return {
        children,
        mcpId,
        name,
        props,
        testID,
        text,
        type: (0, naming_1.getComponentType)(fiber),
    };
};
const serializeFiber = (fiber, maxDepth, currentDepth = 0) => {
    if (!fiber || currentDepth > maxDepth)
        return null;
    try {
        return serializeFiberUnsafe(fiber, maxDepth, currentDepth);
    }
    catch {
        return {
            children: [],
            name: (0, naming_1.getComponentName)(fiber),
            props: { __error: 'Failed to serialize' },
            type: (0, naming_1.getComponentType)(fiber),
        };
    }
};
exports.serializeFiber = serializeFiber;
//# sourceMappingURL=serialize.js.map