name: Patch react-android AAR

on:
  push:
    branches: [main]
    paths:
      - 'android-patches/**'
  workflow_dispatch:

jobs:
  build-patched-aar:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Make gradlew executable
        working-directory: android-patches
        run: chmod +x gradlew

      - name: Build and publish patched AAR
        working-directory: android-patches
        run: ./gradlew assemblePatchedAAR publish --no-daemon
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_ACTOR: ${{ github.actor }}

      - name: Upload AAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: react-android-patched
          path: android-patches/build/patched/react-android-0.83.6-e2e.1.aar
