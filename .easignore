# Dependencies
node_modules/
.yarn/
.expo/

# Native folders
ios/
android/

# Build artifacts (FORCE EXCLUDE)
dist/
build/
web-build/
out/
.next/

# Python virtualenvs accidentally landed in the React Native repo
# (combined ~660MB — caused EAS upload to fail with EPIPE on 472MB tarball)
.venv/
venv/
__pycache__/
*.pyc

# Other unrelated subprojects sitting in repo root
mcdc/
idv-quickstart/

# Tooling/IDE state — never needed for an EAS build
.firebase/
.idea/
dataconnect/
dataconnect-debug.log
extensions/
.tamagui/

# Stray test/debug files
test_appwrite.js
google-cloud-sdk/

# Huge Root Backups & Compressed Archives
LotsOfFiles.tgz
*.tgz
*.zip
*.tar.gz
*.blend
*.mp4
*.jks

# SECURE: BLOCK ALL CREDENTIALS & WORK TRACKS
credentials/
conductor/
path/
gemini-cli/
*.p8
*.p12
*.key
*.mobileprovision
*.pem
*.json
!app.json
!eas.json
!package.json
!package-lock.json
!google-services.json
!GoogleService-Info.plist
!src/**/*.json

# Tool version managers & build artifacts (from .gitignore)
rbenv-plugin/
testing-chruby/
mise-oci/
vcpkg/
bats/
gradle/
www.ruby-lang.org/
android-patches/
platform-tools/
gems/
AiMarketingtool-pro-fbaf2fad/
copilot-demo/
auto/
code./
plugin/



# Heavy local-only artifacts (EAS archive size limit is 2 GB)
backup.bundle
.cargo-features-manager-*
ios/Pods
ios/build
ios/DerivedData
externals
codelab-android-compose
agent-skills
maven
mise-oci
vendor
iwyu
swiftly.pkg
simtool
mybundle
my_gem
my-module
bazel-*
.build
github-ci-server
Pods
*.xcarchive
*.ipa

# Security: secrets must never reach the EAS build archive.
# If a future build needs an EXPO_PUBLIC_* value from .env, mirror it in
# EAS environment variables (eas env) instead of shipping the file.
.env
.env.*
*.keychain

# junk dirs (mirror of .gitignore) 2026-06-16
/AiMarketingtool-pro-fbaf2fa/
/all function action/
/foodie/
/nuxt-nightly/
/.expo-shared/
