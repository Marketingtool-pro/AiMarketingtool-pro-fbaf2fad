[tools]
java = "temurin-21.0.5"

[tasks.ci]
description = "Run continuous integration build for Android App"
# Changes context to the native android app directory to execute gradle
run = "cd android && ./gradlew clean test"

